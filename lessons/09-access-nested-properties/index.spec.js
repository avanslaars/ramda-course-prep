import { compose, defaultTo, path, pathOr } from 'ramda'

test('path provides access to nested properties', () => {
  // TODO: Deeply nested object structure
  // TODO: Basic use of `path`
})

test.todo('handle missing piece of path with defaultTo and compose')
test.todo('replace defaultTo solution with pathOr')
